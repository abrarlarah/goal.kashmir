// @ts-nocheck
import React from 'react';

// Theme toggle is hidden â€” dark mode is permanent for premium sports experience.
// Keeping component to avoid import errors elsewhere.
const ThemeToggle = () => {
    return null;
};

export default ThemeToggle;
