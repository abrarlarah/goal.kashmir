rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEMPORARY: Allow all reads and writes to unblock development
      // WARNING: This is insecure for production. Revert to secure rules before deploying.
      allow read, write: if true;
    }
  }
}
